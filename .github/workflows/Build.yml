on:
  push:
    branches:
      - '*'

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - run: docker info

      - name: Docker
        run: |
          docker run -d archlinux > run.info
          docker start $(cat run.info)
          docker exec $(cat run.info) ls -a